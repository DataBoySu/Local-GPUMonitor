name: AI Readme Translator (German)

on:
  workflow_dispatch:
  push:
    paths:
      - 'README.md'

permissions:
  contents: write

jobs:
  translate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Cache Model Weights
        uses: actions/cache@v4
        with:
          path: ./models
          key: qwen3-4b-instruct-2507-v1

      - name: Install Dependencies
        run: |
          pip install llama-cpp-python
          mkdir -p models

      - name: Download High-Fidelity Translation Model
        run: |
          # Downloading Qwen3-4B-Instruct-2507 for strict 1:1 technical translation
          wget -O models/qwen3-4b-instruct.gguf https://huggingface.co/unsloth/Qwen3-4B-Instruct-2507-GGUF/resolve/main/Qwen3-4B-Instruct-2507-Q4_K_M.gguf

      - name: Run Translation Script
        run: python scripts/translate.py

      - name: Commit and Push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: update german translation [skip ci]"
          file_pattern: 'locales/translated_readme.md'
          push_options: '--force-with-lease'