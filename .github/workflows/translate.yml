name: AI Readme Translator (German)

on:
  workflow_dispatch:
  push:
    paths:
      - 'README.md'

permissions:
  contents: write

jobs:
  translate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Cache Model Weights
        uses: actions/cache@v4
        with:
          path: ./models
          key: qwen2.5-coder-1.5b-v1

      - name: Install Dependencies
        run: |
          pip install llama-cpp-python
          mkdir -p models

      - name: Download Technical Translation Model
        run: |
          # Downloading Qwen2.5-Coder-1.5B which is optimized for technical docs and direct output
          wget -O models/qwen2.5-coder-1.5b-instruct.gguf https://huggingface.co/unsloth/Qwen2.5-Coder-1.5B-Instruct-GGUF/resolve/main/Qwen2.5-Coder-1.5B-Instruct-Q4_K_M.gguf

      - name: Run Translation Script
        run: python scripts/translate.py

      - name: Commit and Push
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs: update german translation [skip ci]"
          file_pattern: 'locales/translated_readme.md'
          push_options: '--force-with-lease'